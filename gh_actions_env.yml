channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - pip
  - numpy
  - networkx>=2
  - biom-format
  - pandas>=1
  - scikit-bio
  - statsmodels
  - h5py
  #- python==3.10 ## specified python version is already present in the gh actions conda env 
  - fastspar==1.0.0 ## change fastspar install to newest version
  - gsl==2.7 ## cannot got higher than this version for fastspar install to work!! (also need conda-forge channel and not main)
  - armadillo>=8 ## fastspar requires at least armadillo 7.800.1 so can leave i think 
  - scipy>=1.11.0
  - matplotlib-base>=3.10 ## seaborn is not compatible w matplotlib 3.11+
  - tqdm
  - python-louvain